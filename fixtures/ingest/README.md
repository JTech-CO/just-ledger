# fixtures/ingest — 은행 CSV 골든 픽스처 (M3 DoD 1)

사용자 확정(2026-07-21): **하나은행 · 국민은행 · 토스뱅크** 3사 포맷 패밀리.

| 파일 | 인코딩 | 포맷 특성 |
|---|---|---|
| `kb.csv` | **CP949** | 프리앰블 3줄 + 헤더, 출금/입금 분리 컬럼, 따옴표 천단위(`"4,500"`), 날짜 `2026.06.02 09:14:33`, 전각 문자(NFKC 케이스: `（주）`, `ＣＵ　`) |
| `hana.csv` | **CP949** | 프리앰블 2줄, 출금/입금 분리, 천단위 없음, 날짜 `2026/06/03`, **`△` 음수 표기**(마이너스 잔액) |
| `toss.csv` | **UTF-8 + BOM** | 헤더 즉시 시작, **부호 단일 금액 컬럼**(`-3200`, `+1250`), 날짜 `2026-06-01`, 메모 컬럼 |

- `utf8-src/` 는 리뷰용 UTF-8 원본. CP949 최종본은 `iconv -f UTF-8 -t CP949` 로 생성한다 (컨테이너에서 `make` 아님 — `scripts/` 참조 없이 수동 1회, 커밋된 바이트가 정본).
- `*.expected.json` 은 각 CSV 의 정규화 기대 출력(`contracts/statement-record.schema.json` 배열). **구현 최초 실행 결과를 사람이 검토(금액·날짜·적요·부호) 후 동결**했다. source_hash 는 지문 알고리즘의 회귀 기준이 된다.
- kb 1·2행, toss 1·2행은 **완전 동일 튜플**(같은 시각·금액·상점) — 발생 순번이 지문에 들어가 두 건 모두 보존되는지(정당한 중복 거래 유실 방지)와 재업로드 결정론을 함께 검증한다.
- 인코딩·전각·`△`·부호 표기는 은행마다 다르다(HARNESS M3 주의). 파서는 3종 전부에서 기대 출력과 **완전 일치**해야 한다.
